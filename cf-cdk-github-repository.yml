Description:  "CDK role for LITTIL Github repositories"

Parameters:
  Environment:
    Description : "Environment"
    Type: "String"
    AllowedPattern : "acc|prod"
  Country:
    Description : "Country (e.g. NL)"
    Type: "String"
    AllowedPattern : "NL"
  GithubOrg:
    Type: String
    Default: Devoxx4Kids-NPO
    AllowedPattern: "Devoxx4Kids-NPO"
  FrontendRepoName:
    Description: "Github front-end repository name"
    Type: String
    Default: littil-frontend
    AllowedPattern: "littil-.*"
  BackendRepoName:
    Description: "Github back-end repository name"
    Type: String
    Default: littil-backend
    AllowedPattern: "littil-.*"

Resources:
  CdkFrontendRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName:
        !Sub
        - "LITTIL-${Country}-${Env}-Cdk"
        - Country: !Ref Country
          Env: !Ref Environment
      Description: Role that is allowed to assume CDK roles
      Path: /
      Policies:
        - PolicyName: assumeRole
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: 'sts:AssumeRole'
                Resource:
                  - 'arn:aws:iam::*:role/cdk-*'
      AssumeRolePolicyDocument:
        Statement:
          - Effect: Allow
            Action: sts:AssumeRoleWithWebIdentity
            Principal:
              Federated: !Ref GithubOidc
            Condition:
              StringLike:
                token.actions.githubusercontent.com:sub: !Sub repo:${FrontendRepoName}:*

  CdkBackendRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName:
        !Sub
        - "LITTIL-${Country}-${Env}-Cdk-backend"
        - Country: !Ref Country
          Env: !Ref Environment
      Description: Role that is allowed to assume CDK roles
      Path: /
      Policies:
        - PolicyName: assumeRole
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: 'sts:AssumeRole'
                Resource:
                  - 'arn:aws:iam::*:role/cdk-*'
      AssumeRolePolicyDocument:
        Statement:
          - Effect: Allow
            Action: sts:AssumeRoleWithWebIdentity
            Principal:
              Federated: !Ref GithubOidc
            Condition:
              StringLike:
                token.actions.githubusercontent.com:sub: !Sub repo:${BackendRepoName}:*

  GithubOidc:
    Type: AWS::IAM::OIDCProvider
    Properties:
      Url: https://token.actions.githubusercontent.com
      ThumbprintList: [1234567891234567891234567891234567891234]
      ClientIdList:
        - !Sub https://github.com/${GithubOrg}

Outputs:
  FrontendRole:
    Value: !GetAtt CdkFrontendRole.Arn
  BackendRole:
    Value: !GetAtt CdkBackendRole.Arn
